extends ../common/template
block head
  meta(name='description' content='Logowanie')
  title Panel logowania
block body
  <?php
  |
  | session_start();
  | if (isset($_SESSION['loginSuccessful']) && $_SESSION['loginSuccessful']) {
  |         header('Location: ../html/memes.php');
  |         exit();
  |    }
  | ?>
  h1 Panel logowania
  form(method="post", action='../php/log-in.php')
    p
      label
      |Login
      |
      input(type="text", name="login")
    P
      label
      |Hasło
      |
      input(type="password", name="password")
    | <?php
    | if (isset($_SESSION['errorMessage']) && !$_SESSION['loginSuccessful'])
    |         print("<p style='color: #d00e00;'>$_SESSION[errorMessage]</p>");
    | ?>
    input(type="submit", value="Log in")
